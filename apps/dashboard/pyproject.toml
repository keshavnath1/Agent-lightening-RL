[project]
name = "dashboard-app"
version = "0.1.0"
description = "Streamlit dashboard app wrapper."
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31",
  "streamlit>=1.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
