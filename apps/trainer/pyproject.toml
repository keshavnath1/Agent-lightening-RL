[project]
name = "trainer-app"
version = "0.1.0"
description = "Track B SFT/GRPO/ART training app wrapper."
requires-python = ">=3.10"
dependencies = [
  "agent-contracts",
  "agent-rewards",
  "lightning-bridge",
]

[project.optional-dependencies]
gpu = [
  "accelerate>=0.28",
  "datasets>=2.18",
  "peft>=0.10",
  "torch>=2.0",
  "transformers>=4.40",
  "trl>=0.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
