[project]
name = "rollout-worker"
version = "0.1.0"
description = "Track A rollout worker app wrapper."
requires-python = ">=3.10"
dependencies = [
  "agent-contracts",
  "agent-rewards",
  "langgraph>=0.2",
  "ml-tools",
  "requests>=2.31",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
