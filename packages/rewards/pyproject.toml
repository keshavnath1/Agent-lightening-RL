[project]
name = "agent-rewards"
version = "0.1.0"
description = "Compatibility package for reward and RULER scoring logic."
requires-python = ">=3.10"
dependencies = [
  "agent-contracts",
  "ml-tools",
  "requests>=2.31",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
