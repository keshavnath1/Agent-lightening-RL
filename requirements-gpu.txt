# GPU training/serving environment for Track B TRL GRPO redeploy.
# Install the CUDA-specific PyTorch wheel appropriate for your RunPod image before this file when necessary.
# Example for CUDA 12.4 images:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.4.1
transformers>=4.51,<5.0
accelerate>=0.34,<2.0
peft>=0.15,<0.19
datasets>=3.2,<4.0
trl>=0.18,<2.0
bitsandbytes>=0.45,<0.46
sentencepiece>=0.2
protobuf>=5.27
pandas>=2.2
numpy>=1.26
requests>=2.32
python-dotenv>=1.0
tabulate>=0.9
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
streamlit>=1.35
altair>=5.3
weave
# vLLM is optional for production-style serving and can be image/CUDA sensitive.
# Install it separately if your pod image supports the desired CUDA/PyTorch combination.
# vllm>=0.6
mcp[cli]>=1.0.0
